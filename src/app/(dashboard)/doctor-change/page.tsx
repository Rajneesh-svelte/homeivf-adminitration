import DoctorChange from '@/components/DoctorChange';

const page = () => {
  return (
    <div className="animate-in fade-in duration-500">
      <DoctorChange />
    </div>
  );
};

export default page;
