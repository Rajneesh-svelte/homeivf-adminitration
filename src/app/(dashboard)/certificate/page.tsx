'use client';
import CertificateFormWrapper from '@/components/CertificateFormWrapper';

const page = () => {
  return (
    <div>
      <CertificateFormWrapper />
    </div>
  );
};

export default page;
